export const metadata = {
  title: 'User Management - Admin',
  description: 'Manage all platform users and their verification status',
}

export default function AdminUsersLayout({ children }) {
  return children
}